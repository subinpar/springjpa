spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jpashop?createDatabaseIfNotExist=true&serverTimezone=Asia/Seoul&useSSL=false&characterEncoding=utf8mb4&allowPublicKeyRetrieval=true
    username: root
    password: "subin0730"
    driver-class-name: com.mysql.cj.jdbc.Driver

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
